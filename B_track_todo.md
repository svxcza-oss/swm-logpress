# B 트랙 작업 체크리스트

> 데이터/라벨 트랙(B)의 실행 순서.
> 배경·근거는 `B_track_nezha_analysis.md` 참고.
> 환경설정(VS Code + Claude Code)은 완료 상태에서 시작.

---

## 1. 준비

- [x] `Thunderbird_2k.log` 받기 — GitHub LogHub에서 Raw 다운로드 → `logpress` 폴더
- [x] `B_track_nezha_analysis.md` 같은 폴더에 넣기
- [x] 전체 Thunderbird 다운로드 시작 — LogHub 표 → Thunderbird 행 🔗 → Zenodo (29.6GB, 오래 걸림)
- [x] 디스크 여유 확인

**⚠️ 디스크 주의**: 압축 29.6GB + 압축 해제 40~50GB = 70~80GB 필요.
61GB 수준이면 아래 중 택1:
- `tar -xzOf Thunderbird.tar.gz | head -n 10000000 > tb_10m.log` (전체 해제 없이 앞부분만 추출, 권장)
- 또는 BGL(708MB)로 먼저 파이프라인 완성 후 확장

---

## 2. 형식 파악 (2k 샘플로)

- [x] Claude Code에 `Thunderbird_2k.log` 형식 분석 요청
- [x] 첫 필드 확인: `-` = 정상 / 그 외 = 이상(alert 태그)
- [x] 필드 구조 파악 (timestamp, date, node, 메시지 본문 등)
- [x] 실제 이상 라인 몇 개 눈으로 확인 — 전체 데이터에서 alert_tag 샘플 같이 확인함

---

## 3. 파싱 스크립트 (2k로 개발)

- [x] 스트리밍 파서 작성 — `for line in f`, 통째로 메모리에 올리지 말 것
- [x] 인코딩 예외 처리 — `errors='ignore'` 필수 (실제 로그라 깨진 바이트 있음)
- [x] 이상 라벨 추출 — 첫 필드 `-` 여부로 0/1
- [x] 2k에서 정상 동작 확인

---

## 4. 레벨 기준 정규식 초안

- [x] 메시지 본문에서 `error`/`fail`/`exception` 등 키워드 정규식 작성
- [x] D에게 중간 공유 — "이런 키워드들이 보인다" (확정 아님, 방향 맞추기)

**⚠️ 핵심 구분**: 이상 라벨(첫 필드) ≠ 에러 레벨(본문 키워드).
**"이상인데 본문에 error 키워드가 없는 줄" = 비-ERROR 하드케이스** ← 프로젝트 동기의 핵심

---

## 5. 윈도우 정의 (D와 합의 필수)

- [x] 윈도우 기준 결정 — 시간 기반(1분/5분) 또는 라인 수 기반(연속 N줄) — **Freeze v2 확정**
- [x] **D와 합의** ← 필수 — 완료

**⚠️** Thunderbird는 네즈하와 달리 장애 케이스 구분이 없어 직접 정의해야 함.
내 기준과 D의 채점 기준이 다르면 숫자가 안 맞음.

---

## 6. 전체 데이터로 확장

- [x] 다운로드 완료 확인
- [x] 앞부분만 추출 (1번의 디스크 주의 참고) — 실제로는 `tar -xzOf | Select-String`로 압축 해제 없이 전체 스트리밍
- [ ] 원본 삭제해 디스크 확보 (선택)
- [x] 3~4의 스크립트를 큰 파일에 적용

---

## 7. 하드케이스 4개 숫자 측정 ★진짜 산출물

- [x] 전체 이상(anomaly) 라인 수 — 3,248,239 ⚠️Thunderbird는 근본원인(root cause) 라벨이 없음. alert_tag는 "이상 표시"일 뿐 "원인"이 아니므로 용어를 "원인"에서 "이상"으로 정정함 (아래도 동일)
- [x] **비-ERROR 이상 수(하드케이스)** ← 프로젝트 동기의 핵심 — 14,939 (VAPI 제외 14,909)
- [x] 중간위치(25~75%) 하드케이스 수 — 8,846
- [x] 윈도우당 하드케이스 분포 (0개/1개/다수) — (노드,템플릿,5분) 기준 6,722그룹

**📌 부수 발견 (논문 근거로 기록)**: `killswitch_comparison_normal.csv` 기준, 정상 라벨(label=`-`)인 줄의 **12.7%(1,896/14,939)**가 본문에 `error`/`fail`/`exception` 키워드를 포함함. 즉 키워드 기반 레벨 필터를 그대로 이상 탐지에 쓰면 이 비율만큼 정상을 이상으로 오탐함 — 비-ERROR 하드케이스(레벨 필터가 놓치는 진짜 이상)와 반대 방향의 오탐 사례로, 레벨 키워드 단독으로는 라벨 대용이 될 수 없다는 정량적 논거.

**📌 데이터 품질 검수**: 이상 라인 3,248,238개(파싱 성공분) 중 message 본문이 빈 줄은 77개(0.0024%), 전부 하드케이스 쪽(77/14,939=0.515%)에서만 나옴 — 본문이 비면 error/fail/exception 키워드가 매치될 수 없어 논리상 당연함. 원본 로그를 재대조한 결과 77개 전부 **원본 자체가 `component:` 뒤에 아무 내용 없이 끝나는 진짜 트렁케이션**이며 파싱 버그로 인한 유실은 0건.

---

## 8. 검증 왕복

- [x] 숫자가 이상하면 → **내 파싱/정규식 버그부터 의심** (네즈하 때 파싱 실패 1,900줄 사례) — 2회 수행 (빈 message 77개, 노드명 #N# 679개 원본 재대조)
- [x] 정규식 수정 → 재측정
- [x] 정상이면 → 정규식 확정 (D 합의)

**순서는 일방통행이 아니라 왕복:**
`전처리 → 정규식 초안 → 측정 → (이상하면) 수정 → 재측정 → 확정`

---

## 9. D에게 전달 (3주차 관문)

- [x] 하드케이스 4개 숫자 → D의 킬스위치 GO/NO-GO 판정 재료 — `killswitch_hardcase_only.csv`(하드케이스 14,939) + `killswitch_comparison_normal.csv`(정상 비교군 14,939)로 분리 정리 완료. 컬럼명 `is_cause`→`is_anomaly`로 정정(Thunderbird엔 근본원인 라벨 없음, alert_tag는 이상 표시일 뿐)
- [x] 네즈하 사전조사 결과(`B_track_nezha_analysis.md`) 공유 → D의 5단계 설계에 영향

**킬스위치 GO 조건 (둘 다 만족):**
1. 하드케이스 표본이 통계 가능 규모 (케이스 수백 개 이상)
2. 필터가 놓친 원인이 희귀도·위치 신호로 무작위보다 유의하게 구별됨

---

## 10. 3주차부터: Nezha 착수 ★B의 관문

- [ ] 원인 라벨 → 로그 라인 매핑 구현
- [ ] OnlineBoutique(문구형) / TrainTicket(코드라인형) 파서 각각
- [ ] 파싱 예외 처리 (이중 JSON, 형식 혼재)
- [ ] 38개 케이스 전부 매핑 → D가 채점에 쓸 형태로
- [ ] **11주차 말 D에게 핸드오프** ← 관문

**사전조사로 지형은 이미 파악됨 (유리한 시작). 상세는 `B_track_nezha_analysis.md` 참고.**

---

## 절대 잊지 말 것

1. **Nezha 3주차 착수** — 미루면 11주 관문 붕괴. "일찍 시작 = 일찍 발견"
2. **B + C 겸직 절대 금지** (문서 명시) — 5~11주차 충돌로 Nezha 희생됨
3. **이상 발견 시 순서**: ① 버그부터 배제 → ② 숫자와 함께 즉시 D에게 → ③ 6~7주차에 하면 조정, 11주차에 하면 사고
4. **꼼꼼함** — 라벨 실수는 티 없이 결과 전체를 오염시킴
5. **개발은 작은 걸로, 측정은 큰 걸로** — 2k/BGL로 파이프라인 만들고 전체로 확장
