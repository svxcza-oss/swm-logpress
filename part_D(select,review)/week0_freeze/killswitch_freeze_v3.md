# LogPress · Kill-switch 프리즈 (관문 ①, M1 · 3주차)

**데이터 접근 전 확정·서명·커밋.** 결과를 본 뒤 임계값·규칙 변경 시 사전등록 효력 소멸.

이 문서의 존재 이유: 실험 전 측정 기준을 미리 설계하여, 운 좋게 나온 결과와 구별하고 결과의 논리를 방어하기 위함.

**이 문서를 읽는 법** — 대부분의 내용은 "구현할 코드"가 아니라 "그 코드가 왜 정당한지에 대한 근거"다. 실제로 구현하는 것은 아래 0장의 5가지뿐이며, 나머지 장(3·6·8·9 등)은 그 5가지가 결과 조작이 아님을 증명하는 사전 설계다.

> **📎 분리 문서 안내 (v3_3)**
> AUC·ROC의 정의, Mann-Whitney U 동치, `r = 2A−1` 환산, Cohen 효과크기, Hanley-McNeil 표본수 역산은 **`_검정력_분석_case_count.md`** 로 이관했다. 본 문서는 그 결과값만 인용하며 유도 과정을 중복 설명하지 않는다.
> 해당 내용은 검정력 분석 **§0(개념 정리)** 과 **§1(근거)** 를 참조할 것.

---

> ### ⚠️ v3_3 개정 시 발견된 문서 간 충돌 — 확정 필요
>
> 검정력 분석 문서와 대조한 결과 아래 3건이 어긋난다. **서명 전 반드시 정리할 것.**
>
> | # | 항목 | 본 프리즈(v3_2) | 검정력 분석 §1-c | 조치 |
> |---|---|---|---|---|
> | 1 | 양성 하한 | `양성 ≥ 40` | **`n_pos ≥ 60`** | 검정력 분석이 *"기존 40은 불균형 설계(37) 기준이라 주검정(1:1, 57)에 미달"* 이라고 **명시적으로 기각**함 → **60으로 상향** (본문 반영) |
> | 2 | 검정 방향 | H₁ `AUC > 0.5` **one-sided** | `α = 0.05` **양측** (`z₁₋α/₂ = 1.96`) | 표본수 57·37이 **양측으로 산출**됨. 단측 선언은 근거와 불일치 → **양측으로 통일 권장** |
> | 3 | 관측 단위 수 | 6,685 (CHK_DSK·#N# 제외 후) | 6,722 (제외 전) | 둘 다 맞으나 기준이 다름 → 검정력 분석 표를 **6,685 기준으로 갱신**하거나 각주 병기 |

---

## 0. 한눈에 보기

### 실제 구현 (코드로 만드는 것 — 5가지)

| # | | 구현 내용 |
|---|---|---|
| 1 | 희귀도 계산 | 템플릿이 전체 로그에서 몇 번 나왔는지 세기 |
| 2 | 위치 계산 | 줄이 로그 뭉치에서 어디쯤(0~1)인지 |
| 3 | 음성 매칭 | 같은 노드·±1시간에서 평범한 줄 뽑기 |
| 4 | AUC 측정 | 희귀도+위치로 양성/음성 구별하는 정도 |
| 5 | 순열검정 | 그 결과가 우연이 아닌지 5,000회 검정 |

### 나머지 문서 내용

위 5가지가 왜 정당한지 방어하는 근거다. 순환 방지(3장), 의사복제 방어(6장), 교란 통제(8장), 재현성(9장)은 모두 "결과를 조작하지 않았음"을 사전에 못 박는 설계다.

**최종 판정** — AUC ≥ 0.65 & 순열검정 p < 0.05 → **GO**, 아니면 **NO-GO**(신호 재설계 또는 주제 리셋).

---

## 1. 가설

원인 줄을 찾는 단서 두 개를 합쳐 점수 하나로 만든 것이 **결합 신호**(희귀도 + 위치). (4장 참고)

- **희귀도(rarity)**: 이 로그 문구가 전체에서 몇 번 나왔나 카운팅. 드물수록 원인일 가능성이 높다는 가설.
- **위치(position)**: 로그 뭉치를 0~1로 놓고 이 줄이 어디쯤인지. 맨 앞 0, 한가운데 0.5, 맨 끝 1.

**둘 다 쓰는 이유:** 희귀도는 템플릿마다 값이 하나로 정해진다(같은 템플릿이면 어디서 나오든 희귀도 동일). 양성 템플릿이 9종뿐이라 희귀도 점수가 9가지밖에 안 나온다. 관측 단위는 6,685개인데 실제 구별되는 값은 9개. 그래서 위치를 붙여 촘촘하게 만든다.

| | 가설 내용 |
|---|---|
| **H₀** (귀무) | 결합 신호가 원인 줄을 평범한 줄과 구분하지 못한다 (AUC = 0.5, 동전던지기). |
| **H₁** (대립) | 결합 신호가 원인 줄에 더 높은 점수를 준다 (AUC > 0.5). ← 우리가 주장할 것 |

> **검정 방향:** ⚠️ 충돌 #2 참조. 표본수 근거(57·37)가 **양측 α=0.05**로 산출되었으므로 양측으로 통일할 것을 권장한다.

> **AUC 정의·`0.65`의 근거** → **검정력 분석 §0.2, §1-a** 참조.
> (요지만: AUC = 양성 하나와 음성 하나를 무작위로 집었을 때 양성 점수가 더 높을 확률. 0.65 → `r_rb = 0.30` = Cohen 중간 효과 하한과 **등식**으로 일치.)

---

## 2. GO 판정식

| # | 조건 | 임계값 | 실측 | 판정 |
|---|---|---|---|---|
| **①-a 총 표본** | **관측 단위(=버스트) 총수 `n_eval` ≥ 300** | 300 | 6,685 (1:1 시 13,370) | ✅ 22배 |
| **①-b 양성** | **양성 관측 단위 `n_pos` ≥ 60** ⚠️(구 40) | **60** | 6,685 | ✅ 111배 |
| **①-c 음성** | 음성 관측 단위 `n_neg` 확보(매칭) | 양성의 1배↑ | 매칭 6,652 · 폴백 70 | ✅ 재추출 |
| **② 판별력** | 1:1 AUC ≥ 0.65 & 순열검정(5,000회) p < 0.05 | 0.65 | 측정 예정 (D) | 대기 |
| **②-강건** | 1:4 AUC ≥ 0.65 & 1:1과의 차 ≤ 0.05 | 0.65 | 측정 예정 (D) | 대기 |
| 보조 | level-filter 보존율 < 0.99 | 0.99 | 하드케이스 100% 미포착 | ✅ 유리 |

**용어 대응 (검정력 분석과 동일 표기):**

| 본 문서 | 검정력 분석 표기 | 값 |
|---|---|---|
| 독립 표본 / 버스트 1개 | **관측 단위** (ROC 곡선 위의 점 1개) | — |
| 양성(원인 줄 = 하드케이스) 수 | **`n_pos`** | 6,685 |
| 음성(평범한 줄) 수 | **`n_neg`** | 1:1 시 6,685 |
| 총 표본 수 | **`n_eval`** = `n_pos + n_neg` | 13,370 |
| 양성 비율 | **양성 구성비** = `n_pos / n_eval` | 50% (매칭비가 결정) |

> ※ 실측 6,685 = (노드,템플릿,5분) 버스트 6,722에서 CHK_DSK 13 + `#N#` 31(버스트로는 37개 소멸)을 제외한 값. 5장 참조.
> ※ **`n_pos ≥ 60` 근거** → 검정력 분석 §1-b·§1-c. 1:1 주검정 요건 57에 안전 여유를 얹은 값.

①은 **잴 자격**을 보장하고, ②는 **실제 성능**을 잰다. ②와 ②-강건은 **같은 실행에서 함께 측정**한다. 1:1을 재고 결과를 본 뒤 1:4를 재면 유리한 쪽을 골랐다는 의심을 피할 수 없다. 두 조건 AUC가 비슷하면 결과가 표본 비율에 흔들리지 않는다는 뜻(로버스트).

---

## 3. 라벨 vs 신호 (순환 방지)

**순환이란?** 시험 문제를 내면서 답을 문제에 적어놓는 것. 정답을 "위치"로 정해놓고 "위치"로 맞히면, 당연히 맞지만 의미가 없다(답 보고 답 쓰기).

| 구분 | 사용 축 | 위치 포함? |
|---|---|---|
| 채점 기준 (정답 라벨) | alert 태그만 | ❌ 제외 |
| 정답을 맞추는 신호 | 희귀도 + 위치 | ✅ 포함 |

**alert 태그란:** 로그 줄 맨 앞의 이상 표시. `-`이면 정상, `PBS_CON`·`SCSI`·`CPU` 등이면 이상. Thunderbird 제작자가 내용을 보고 미리 붙인 것이라 위치와 무관하다.

```
이전 판(순환):
  정답 정의: "중간(0.25~0.75)에 있는 줄 = 원인"
  신호:      "중간에 있으면 원인일 것"
  → 위치로 정하고 위치로 맞힘 = 무의미. (b) 조항 삭제.

현재(순환 차단):
  정답 정의: "alert 태그 붙은 줄 = 원인" (위치 상관없음)
  신호:      "중간에 있으면 원인일 것" (위치 사용)
  → 정답은 위치를 모른 채 정해졌고, 신호가 위치로 맞힘
```

**"59.2% 중간 집중"이 왜 라벨이 아니라 관측 결과인가**
태그로 하드케이스를 정한 뒤(위치 안 봄), 세어보니 그것들의 59.2%가 중간에 있더라는 것. "중간에 있어서 하드케이스로 정한" 게 아니라 "태그로 정했더니 알고 보니 59.2%가 중간이더라"는 관측이다. 정답을 위치로 정했다는 근거가 아니라, **위치 신호가 통할 것이라는 유리한 정황**일 뿐이다.

**데이터 검증:** `is_anomaly`/`is_hardcase`가 `alert_tag`로 완전 결정되며, `position_ratio`는 라벨 규칙에 미포함(예외 0건).

---

## 4. 신호 정의

| 신호 | 정의 | 특성 |
|---|---|---|
| **희귀도** | 템플릿이 전체 로그에서 몇 번 나왔는가. 드물수록 높은 점수. 200만 번 나온 흔한 문구는 낮고, 5번 나온 문구는 높음. | 템플릿당 1개 값 → 9가지 한계 |
| **위치** | 로그 뭉치 0~1 기준 상대 위치 (앞 0, 중앙 0.5, 끝 1). | 버스트마다 달라짐 |

**결합 이유:** 희귀도는 같은 템플릿이면 값이 동일해 9종뿐이면 9가지 값만 나온다. 위치는 같은 템플릿이라도 버스트마다 값이 달라 촘촘함을 확보한다. 그래서 둘을 결합해 검정한다.

**잔여 리스크:** 위치 신호는 "중간=원인" 가정이 데이터 의존적. 원인이 로그 꼬리에 있는 롱테일 케이스엔 약할 수 있음. 실측 AUC가 낮으면 NO-GO 대응(11장)으로 이행.

---

## 5. 표본 단위

**(노드, 템플릿, 5분 시간창) 하나 = 관측 단위 1개(=버스트 1개 = ROC 곡선 위의 점 1개).** 같은 노드에서 같은 템플릿이 5분 안에 연달아 나오면 1개, 5분 이상 잠잠했다 재발하면 새로 1개. 셋 중 하나라도 다르면 별개 표본.

> **버스트 = (노드·템플릿·5분) = 관측 단위** — 셋은 같은 것의 다른 이름이다. "버스트"는 통칭("같은 에러가 몰린 덩어리"), "(노드·템플릿·5분)"은 그것의 정확한 정의, **"관측 단위"는 검정력 분석 문서의 표기**다. 로그 줄(14,939)을 이 규칙으로 묶으면 버스트(6,722)가 된다.

**템플릿이란:** 로그 문구에서 변하는 부분(숫자·ID)을 `<*>`로 지우고 뼈대만 남긴 것(Drain3 도구 사용). 예: `node-042 disk sda1 I/O error` 등 3줄 → `node-<*> disk <*> I/O error` 1개. 겉보기 1.5만 줄이 실제로는 9종 문구.

| 기준 | 없으면 생기는 문제 |
|---|---|
| 노드 | 다른 서버 일을 같은 사건으로 착각 |
| 템플릿 | 디스크 에러와 CPU 에러를 한 덩어리로 뭉갬 |
| 5분 | 어제 일과 오늘 일을 같은 사건으로 셈 |

- 실측 **6,722**개. ±1시간 매칭 성공 6,652, 실패 70.
- **제외:** CHK_DSK 13개(단일 노드 전용), `#N#` 31개(플레이스홀더 → 노드 매칭 불가). 라인 44개가 버스트로는 37개 소멸(CHK_DSK 13줄이 6버스트로 뭉침, `#N#` 31줄은 안 뭉침). → **실제 모집단 6,722 − 37 = 6,685.** 사유·개수 기록.

---

## 6. 의사복제 방어

**의사복제** = 가짜로 표본이 많아 보이는 현상. 디스크 1개 고장에 로그 500줄이 찍히면, 줄로 세면 500개지만 실제 사건은 1개. 이를 막기 위해 줄 단위(14,939)가 아니라 **관측 단위(버스트, 6,685)**로 센다.

- 명목 라인 14,939개 사용 금지. Drain3로 묶으면 템플릿 9종, 상위 4종이 99.2%(14,813개).

**예상 반박: "9종뿐이면 사건도 9개 아닌가?" — 독립성 근거로 방어**
같은 템플릿이라도 **3,085개의 서로 다른 노드 · 211개의 다른 날짜**에 분산 발생. 상위 15개 노드 합계도 38%뿐(특정 서버 쏠림 없음). 즉 같은 종류 에러라도 다른 기계·다른 날 각각 일어난 독립 사건이므로 9개가 아니다.

rarity 단독 AUC는 9종 한계를 명시해 **보조로만** 보고한다.

---

## 7. 양성 정의

**양성 = alert 태그가 붙어 있고 본문에 error류 단어가 없는 줄** — 14,939개(VAPI 제외 시 14,909개).

- **non-ERROR 조건 이유:** 본문에 error가 있는 줄은 기존 레벨 필터가 이미 잡는다. 우리 대상은 필터가 놓치는 **숨은 원인**.
- **VAPI 제외:** 322만 줄로 흔하고 레벨 필터가 처리. 하드케이스에 30줄만 포함되어 영향 없음.

---

## 8. 음성 & 매칭 규칙

**왜 재추출하나:** 현행 비교군은 무작위 균등 추출이라 양성과 조건이 안 맞는다(position 0~1 균등, 시간 8개월 전 구간, 노드 3,876개로 불일치). 조건이 다르면 교란이 생겨 공정한 비교가 안 된다.

### 핵심 개념: 교란 & 교란 통제

```
교란 = 진짜 원인이 아닌데 결과에 영향 주는 것
  양성은 장애 시간대·특정 노드에 몰림.
  음성을 아무 데서나 뽑으면
  → "이 줄 03:22(장애 시각)네 → 양성!" 처럼 시간대만 보고 맞힘.
    AUC가 높아도 "신호가 좋아서(우리 실력)"인지
    "시간·노드가 달라서(교란)"인지 구별 불가.

교란 통제 = 시간·노드를 양쪽 똑같이 맞춰 상쇄.
  → 조건이 같으니 희귀도·위치로만 구별하게 됨 = 진짜 시험.
```

| 항목 | 처리 & 이유 |
|---|---|
| **맞출 것** | 같은 노드, ±1시간 이내 → 시간·노드 교란을 양쪽에서 상쇄 |
| **반대로 둘 것** | 음성은 alert 태그 `-`인 평범한 줄 → 조건은 같고 정답만 반대 |
| **건드리지 말 것** | position 매칭 금지 — 위치는 측정 대상. 양쪽 위치까지 같게 맞추면 위치로 구별할 게 없어져 신호가 죽는다 |
| **세는 단위** | 양성과 동일 (노드, 템플릿, 5분) = 관측 단위 |
| **비율** | 1:4로 뽑되 첫 번째가 1:1용. 따로 뽑으면 두 집합이 달라져 비교 불성립 |
| **음성 내 ERROR** | non-ERROR(INFO)로 한정 (현행 파일에 1,896개 혼입) |

### 후보 모으기 vs 후보 선택 — 2단계

```
1단계 (후보 모으기): "같은 노드 + ±1시간 + 평범한 줄"을 다 긁어모음
  → 후보 수십~수백 개 (평균 524, 중앙값 26)

2단계 (후보 선택): 그중 무작위로 1개(1:1) 또는 4개(1:4)
  → "가장 가까운 것"은 안 씀: 양성 직전·직후 줄이면
     같은 장애에 오염됐을 수 있음(정상 아닐 수도).
     교란 통제는 창 자체가 보장하므로 창 안에서는 무작위가 적절.
```

### 시간 범위 = ±1시간 (실측 근거)

| 범위 | 성공 | 실패 |
|---|---|---|
| 같은 5분 | 14,478 (96.9%) | 461 |
| ±30분 | 14,846 (99.4%) | 93 |
| **±1시간 (채택)** | **14,869 (99.5%)** | **70** |
| ±6시간+ | 14,939 (100%) | 0 |

- **같은 5분 탈락:** 실패 461개가 "정상 로그 없이 갑자기 튄" 케이스라 오히려 가장 원인다울 수 있음.
- **±6시간+ 탈락:** 결과는 같은데 교란 통제만 느슨해짐. → ±1시간이 균형점.

### 매칭 실패 = 제외 금지 (선택 편향 방지), 2단계 폴백

**선택 편향** = 표본 고르는 방식 때문에 결과가 왜곡되는 것. 실패 70개는 "주변에 정상 로그가 없다 갑자기 튄" 케이스로 가장 원인다울 수 있어, 버리면 결과가 왜곡된다.

1. ±1시간 · 같은 노드 매칭
2. 실패 시 → 같은 노드 전체 기간으로 시간 제약만 완화 (노드는 유지)
3. 그래도 실패 → 제외하지 말고 민감도 분석에서 별도 집계·보고

**레벨필터 ERROR를 음성으로 안 쓰는 이유:** "숨은 원인 vs 뻔한 에러"를 묻게 되어 킬스위치 질문과 어긋나고, rarity가 자동으로 갈려 AUC가 부풀려짐. 서술적 근거로만 사용.

---

## 9. 검정 & 재현성

| 용도 | seed | 반복 | 산출물 |
|---|---|---|---|
| 음성 매칭 추출 | `seed_negative = 42` | — | `negatives_matched.csv` |
| 순열검정 | `seed_perm = 7` | **5,000회 고정** | `perm_null_dist.npy` |

### 순열검정이란

```
"이 정도 점수가 우연히 나올 수 있나?"를 실험으로 확인.
  1. 정답 라벨을 무작위로 섞음 (신호와 정답의 관계를 끊음)
  2. 그 상태로 AUC 계산
  3. 5,000번 반복 → "우연 점수" 5,000개 (귀무분포)
  4. 진짜 AUC가 이 분포보다 확실히 높으면 → 우연 아님
     p < 0.05 = 우연히 이 점수가 나올 확률이 5% 미만
```

### 왜 하필 5,000회인가 — 세 가지 요건의 교집합

순열검정의 p값은 **유한 반복으로 추정한 값**이므로 자체 오차(몬테카를로 오차)를 갖는다. 반복수 `B`를 정하는 것은 그 오차를 어디까지 허용하느냐의 문제다.

**요건 ① — p = 0.05 부근에서 판정이 흔들리지 않을 것**

`p ≈ 0.05`에서 몬테카를로 표준오차는 `SE = √(p(1−p)/B)`이다.

| B | SE | 95% 신뢰구간 | p=0.05가 놓이는 범위 |
|---|---|---|---|
| 1,000 | 0.0069 | ±0.0135 | [0.037, 0.064] ← **0.05와 0.06 구분 불가** |
| 2,000 | 0.0049 | ±0.0096 | [0.040, 0.060] |
| **5,000** | **0.0031** | **±0.0060** | **[0.044, 0.056]** ✅ |
| 10,000 | 0.0022 | ±0.0043 | [0.046, 0.054] |

**0.05와 0.06의 차이(0.01)가 SE의 몇 배인가**로 보면 — B=1,000에서 1.4배(구분 불가), **B=5,000에서 3.1배**(안정적 구분), B=10,000에서 4.4배. 즉 **5,000에서 판정 안정성이 확보**되고, 그 이상은 비용 대비 이득이 급감한다.

**요건 ② — 최소 표현 가능 p값이 판정 임계값보다 충분히 작을 것**

순열검정의 p값은 `(귀무분포 중 실제 AUC 이상인 개수 + 1) / (B + 1)`이므로, 아무리 강한 신호라도 **`1/(B+1)` 미만으로는 내려갈 수 없다.**

| B | 최소 표현 가능 p |
|---|---|
| 1,000 | 0.0010 |
| **5,000** | **0.0002** |
| 10,000 | 0.0001 |

B=5,000이면 최소 p = 0.0002로, 판정선 0.05보다 **250배 아래**까지 표현된다. 다중비교 보정(BH-FDR) 후 기준이 조여져도 해상도가 남는다.

**요건 ③ — 재현 가능한 시간 안에 끝날 것**
B를 10,000으로 올리면 계산 시간이 2배가 되지만 신뢰구간은 ±0.006 → ±0.0043으로만 줄어든다. 판정 결론이 바뀌지 않는 개선에 비용을 두 배 쓰는 셈이다.

> **결론:** 5,000은 "관례"가 아니라 **① 판정 안정성 확보 · ② 충분한 p 해상도 · ③ 합리적 계산 비용**이 동시에 만족되는 최소 지점이다.
> **다만 가장 중요한 것은 숫자 자체가 아니라 "미리 정하고 결과를 본 뒤 바꾸지 않는 것"이다.** p값이 애매하다고 반복을 늘리는 것은 optional stopping = p-해킹이다.

### p-해킹 / optional stopping 방지

- **p-해킹:** 여러 방법으로 돌려보다 유의한 것만 보고. **optional stopping:** p값이 애매하면 반복·표본을 늘려 재시도. 둘 다 "우연히 잘 나온 것"을 진짜처럼 만든다.
- **방지책:** 순열 5,000회 고정, seed 고정, 규칙 먼저 커밋. 반복·seed는 결과를 보고 바꾸지 않는다.
- **인덱스 파일이 최종 증거** — seed만으론 라이브러리 버전 변화 시 재현이 깨질 수 있음. 추출된 `line_id` 목록·귀무분포를 파일로 남긴다.
- `requirements.lock`에 numpy/pandas/scipy 버전 고정 후 함께 커밋. **1회 실행 원칙**(재실행 시 사유·시각·변경점 기록, 이전 결과 보존).
- **현재 AUC 미측정(깨끗함).** 매칭 규칙 확정·커밋 → 음성 재추출 → AUC 측정 순서 준수.

---

## 10. 임계값 근거

> **표본수(`n_pos` 60 · `n_eval` 300) 및 AUC 0.65의 근거** → **검정력 분석 §1-a, §1-b, §1-c** 로 이관.
> 본 장은 검정력 분석이 다루지 않는 임계값만 다룬다.

| 임계값 | 근거 |
|---|---|
| **p < 0.05** | Fisher(1925) 《Statistical Methods for Research Workers》의 "20분의 1" 기준. Fisher 본인도 임의적 절단점임을 명시. |
| **순열 5,000회** | 몬테카를로 오차 기준. p=0.05 부근 95% CI ±0.006, 최소 표현 p = 0.0002. 9장 참조. |
| **±1시간** | 실측: 99.5% 매칭, 실패 70. 5분은 461 손실, 6시간은 교란 느슨. 8장 참조. |
| **5분 버스트 창** | 자의적 선택이나 강건함. 1~30분 어느 창이든 6,233~7,528개(최대·최소차 17%)로 전부 GO 기준의 20배 이상. 부록 B 참조. |
| **level-filter 0.99** | 기존 방법으로 99% 이상 살아남으면 프로젝트 의미 없음. 하드케이스는 100% 미포착이라 넉넉히 통과. |

---

## 11. NO-GO 대응

- **표본 부족** → BGL(다른 로그 데이터셋) 병합 / 층화 완화 / 라인·버스트 혼합 검정.
- **신호 무력 (AUC ≈ 0.5)** → rarity를 엔트로피·시간 간격·버스트성으로 재설계, 또는 주제 리셋.

---

## 12. 서명 (데이터 접근 전)

| 트랙 | 역할 | 서명 | 날짜 |
|---|---|---|---|
| A | 인프라 | | |
| B | 데이터 | | |
| C | 매핑/압축 | | |
| D | 리드/평가 | | |

**절차:** 4인 서명 → git commit → 해시 기록 → 그 다음에 데이터 접근.

---

## 부록 A. 용어 정리

> AUC·ROC·효과크기 관련 용어는 **검정력 분석 §0.4 용어 대응표** 참조.

| 용어 | 뜻 |
|---|---|
| **alert 태그** | 로그 줄 맨 앞의 이상 표시 자리를 통칭. `-`(정상), `SCSI`·`CPU`·`PBS_CON` 등(이상). 위치와 무관, 제작자가 붙임. |
| **양성 / 음성** | 양성=원인 줄(하드케이스, `n_pos`), 음성=평범한 줄(비교군, `n_neg`). |
| **버스트 / 관측 단위** | 같은 로그가 짧은 시간에 몰린 덩어리. = (노드,템플릿,5분)으로 묶은 표본 1개. **검정력 분석에서는 "관측 단위"로 표기.** |
| **`n_pos` / `n_neg` / `n_eval`** | 검정력 분석 표기. 각각 양성 수 / 음성 수 / 총 표본 수(`n_pos + n_neg`). |
| **양성 구성비** | `n_pos / n_eval`. 매칭 비율이 결정(1:1 → 50%, 1:4 → 20%). |
| **노드** | 컴퓨터 한 대 (Thunderbird는 수천 대). |
| **템플릿** | 로그 문구에서 변하는 부분을 `<*>`로 지운 뼈대 (Drain3). 14,939줄 → 9종. |
| **순환** | 정답을 정하는 축과 맞추는 축이 겹치는 것(답을 문제에 적어놓음). 위치를 라벨에서 빼 차단. |
| **교란** | 진짜 원인이 아닌데 결과에 영향 주는 변수(시간·노드). 양쪽 맞춰 상쇄(교란 통제). |
| **선택 편향** | 표본 고르는 방식 때문에 결과가 왜곡되는 것. 매칭 실패분을 버리면 발생 → 제외 금지. |
| **의사복제** | 가짜로 표본이 많아 보이는 현상. 줄이 아니라 관측 단위로 세어 방지. |
| **순열검정** | 정답을 5,000번 섞어 "우연 점수"를 만들고, 진짜 점수가 그보다 높은지 검정. |
| **p < 0.05** | 우연히 이 점수가 나올 확률이 5% 미만. |
| **seed** | 무작위로 섞는 순서를 정하는 번호. 고정하면 재현 가능. |
| **민감도 분석** | 조건(예: 실패 70개 포함/제외)을 바꿔도 결론이 같은지 확인. |
| **로버스트** | 튼튼함. 조건(1:1↔1:4)을 바꿔도 결과가 안 흔들림. |

---

## 부록 B. 수치 근거표

교수·심사자가 어떤 수치를 짚어도 답할 수 있도록 정의·근거·강도를 정리한다.
**강도:** 문헌(논문 인용) / 관례(관행 인정) / 자의적(강건성·보수성으로 방어) / 실측(재현 가능).

> **AUC 0.65 · `n_pos` 60 · `n_eval` 300 · 효과크기 0.30** 의 근거는 **검정력 분석 §1** 로 이관. 질문 시 그 문서를 인용한다.

| 수치 | 정의 | 근거 | 강도 |
|---|---|---|---|
| **p<0.05** | 유의수준 | Fisher 1925. "20분의 1", 본인도 임의적 절단점 인정 [C-4] | 관례 |
| **5,000** | 순열 반복 | 몬테카를로 오차: p=0.05에서 95% CI ±0.006, 최소 p=0.0002. 9장 | 실측 방어 |
| **±1시간** | 매칭 범위 | 실측: 99.5% 매칭, 실패 70. 5분은 461 손실, 6시간은 교란 느슨 | 실측 |
| **5분** | 버스트 창 | 자의적 선택. 1~30분 어느 창이든 6,233~7,528개로 300의 20배↑, 최대·최소차 17% (아래 강건성 표) | 실측 방어 |
| **0.99** | 보존율 임계 | 임의 임계. 실제 100% 미포착이라 넉넉히 통과 | 자의적 |
| **6,685** | 관측 단위 수 (`n_pos`) | 실측. 6,722에서 CHK_DSK·`#N#` 제외(37 소멸) 반영 | 실측 |
| **14,895** | 하드케이스 라인 | 실측 (CHK_DSK·`#N#` 제외 반영. VAPI 제외 기준) | 실측 |
| **9종 / 99.2%** | 템플릿 집중 | 실측 (Drain3 묶음, 상위 4종) | 실측 |
| **59.2%** | 중간 집중 | 실측 (관측 결과, 라벨 아님) | 실측 |
| **3,085 / 211** | 노드 / 날짜 | 실측 (독립성 근거). 상위 15노드 38% | 실측 |

### 버스트 창 강건성 (양성 14,895, 노드 포함 실측)

| 창 크기 | 관측 단위 (노드,템플릿,N분) | GO 기준(300) 대비 |
|---|---|---|
| 1분 | 7,528 | 25배 |
| 2분 | 7,017 | 23배 |
| **5분 (채택)** | **6,685** | **22배** |
| 10분 | 6,346 | 21배 |
| 30분 | 6,233 | 21배 |

→ 창을 바꿔도 표본이 6,233~7,528(최대·최소차 17%)로 전부 GO 기준의 20배 이상. **"5분이 최적"이 아니라 "창 선택이 결론에 영향 없음(강건)"이 방어 논리.** 순서(1분>5분>30분)도 정상.

### 대응 원칙

- **문헌**(p<0.05) → 저자·연도 인용.
- **검정력 분석 이관분**(0.65·60·300·0.30) → **검정력 분석 문서 §1** 인용.
- **관례/자의적**(5분·0.99) → "자의적이나 강건성·보수성으로 결론 불변".
- **실측**(6,685·14,895 등) → "측정값, seed 고정으로 재현 가능".

---

## 부록 C. 논문 출처 & 링크

> Hanley & McNeil(1982), Kerby(2014), Cohen(1988/1992), Vargha & Delaney(2000)은 **검정력 분석 문서**에서 인용·관리한다. 본 문서는 아래만 유지한다.

| 번호 | 문헌 | 출처 & 링크 |
|---|---|---|
| **[C-4]** | Fisher (1925)<br>p < 0.05 유의수준 | *Statistical Methods for Research Workers*. Oliver & Boyd, Edinburgh.<br>아카이브 (York Univ.)<br>"P=0.05 = 1 in 20"의 편의적 기준. 본인도 임의적 절단점 명시 |

**인용 원칙:** 어느 수치를 짚어도 **"문헌 / 검정력 분석 이관 / 관례 / 자의적 / 실측"** 중 하나로 위치를 명확히 답한다.

---

**측정 출처:** Thunderbird 전체 2억 1,121만 줄 전수 스캔(2005-11-11 ~ 2006-07-11, 243일), B 트랙, parse error 0.
**비교군 검증:** `killswitch_comparison_normal.csv`(14,939행) = 무작위 균등 추출 → 재추출 대상.
